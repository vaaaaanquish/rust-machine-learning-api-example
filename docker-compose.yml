version: "3.7"

services:
  app:
    build:
      context: .
    container_name: rustapi
    working_dir: /app
    command: cargo run
    tty: true
    volumes:
      - ./:/app
    ports:
      - 3000:3000
